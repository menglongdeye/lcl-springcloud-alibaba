server:
  port: 8081
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.20.10.2:8848
  application:
    name: provider01-depart
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: none
  # ?????
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://172.20.10.14:3306/test?useUnicode=true&amp;characterEncoding=utf8
    username: root
    password: root

# ????
logging:
  pattern:
    console: level-%level %msg%n
  level:
    root: info
    org.hibernate: info
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    org.hibernate.type.descriptor.sql.BasicExtractor: trace
    com.abc: debug
